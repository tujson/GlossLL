library constants;

import 'package:flutter/material.dart';

const DARK_MODE_SETTING = "dark_mode_setting";
const DARK_MODE_SYSTEM = "ThemeMode.system";
const DARK_MODE_ON = "ThemeMode.dark";
const DARK_MODE_OFF = "ThemeMode.light";

class GlossLlColors {
  static const Color primaryColor = Color(0xFF76EE00);
}

enum ProficiencyLevel { beginner, intermediate, advanced }
